extension Repository.Policy {
    /// Fleet uniformity: the canonical package shape policy every active
    /// public package repository carries identically (swift-institute/
    /// .github#600).
    public enum Uniformity {}
}

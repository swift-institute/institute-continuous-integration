import Institute_Continuous_Integration

// Nest.Name namespace shell. `Institute.ContinuousIntegration.Receipt`
// owns canonical evidence, same-run identities, and completion/runtime
// readiness — the record a control-plane run leaves behind, and the
// bootstrap identity that proves the binary producing it came from the
// sources it claims.
extension Institute.ContinuousIntegration {
    public enum Receipt {}
}

# Synthetic workflow — workflow_call:/workflow_dispatch: only; missing
# push: and pull_request:. Would fire check 5 if the manifest entry
# were self-firing: active, but it's deferred so check 5 is exempted.
name: validate-stub

on:
  workflow_call: {}
  workflow_dispatch: {}

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - run: echo synthetic

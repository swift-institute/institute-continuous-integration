#!/usr/bin/env python3
"""Fixture stub — validate-stub.py. Referenced by TEST-CHECK5-EDGE in
the synthetic manifest. The binding validator only checks file
existence + reference, not script content. Check 5 reads the workflow,
not this script."""

# Widget Server

Workflow scope for the swift-institute example org. Exactly one real H1; the
`#` lines below live inside a fenced shell block and must not be counted.

```bash
# Required
export WIDGET_TOKEN=...

# Visit http://localhost:8080 to confirm the server is up
swift run Server
```

// Non-L2 package — does NOT import platform C. Uses Kernel/IO/etc.
// from the platform stack instead.

import Linter_Primitives  // not real here, just shape

public enum Thing {
    public static let name: Swift.String = "thing"
}

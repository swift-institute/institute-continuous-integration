// Not a linter rule — basename does not match `Lint.Rule.*.*.swift`.
// The validator skips this file; [API-NAME-009] does not fire.

public enum Helper {
    static let message: String = "any free-form prose is acceptable here"
}

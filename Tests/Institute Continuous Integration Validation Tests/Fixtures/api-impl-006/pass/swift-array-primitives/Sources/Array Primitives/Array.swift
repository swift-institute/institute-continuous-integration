// `Array.swift` is single-word; not compound; basename is fine.
// (This is a placeholder companion file; in production [API-IMPL-005]
// allows one type per file, which Array would be.)

extension Array {
    @inlinable
    public func dummy() {}
}

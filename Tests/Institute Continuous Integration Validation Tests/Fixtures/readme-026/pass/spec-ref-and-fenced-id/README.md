# Widget Process

This workflow implements the HTTP semantics of [RFC-7231] and the date format of
[ISO-8601]. External specification citations are not internal rule IDs, so they
are permitted in published prose.

A multi-segment internal ID is fine inside a fenced code block, which the scan
strips before checking:

```text
See [PLAT-ARCH-008e] in the internal architecture notes.
```

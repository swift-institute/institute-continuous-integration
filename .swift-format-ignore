# Deliberately defective validator fixture corpus: fail/ scenarios exist to
# violate the rules their validators detect. Formatting a corpus of
# intentional violations, or failing on their style, is not a finding about
# this repository's code. Mirrors the SwiftLint `excluded:` exclusion in
# .swiftlint.yml.
#
# swift-format's .swift-format-ignore (release/6.4.x) does not honor `**`
# mid-pattern glob segments in local testing — `Tests/**/Fixtures/**`,
# `**/Fixtures/**`, and `Fixtures/**` all failed to match; only a fully
# explicit path per fixture root does. Listed individually. See #4.
Tests/Repository Policy Tests/Fixtures/**
Tests/Institute Continuous Integration Validation Tests/Fixtures/**
